State Department Extends Visa Ban on Abbas, Other Palestinian Officials

The move would prevent Mahmoud Abbas, the leader of two governing Palestinian groups, from traveling to the United States next week for the United Nations General Assembly.# steps in commands
mkdir sample_demo
git init
git remote add origin
git remote -v
git branch
git branch -M branc_name
